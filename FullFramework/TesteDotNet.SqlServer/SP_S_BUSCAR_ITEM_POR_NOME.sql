CREATE PROCEDURE SP_S_BUSCAR_ITEM_POR_NOME
(
	@NOME_ITEM VARCHAR(40)
)
AS
BEGIN
	SELECT
		*
	FROM
		CATEGORIA	
	INNER JOIN
		ITEM			
	ON
		CATEGORIA.ID = ITEM.CATEGORIA_ID 
	WHERE
		ITEM.NOME LIKE '%'+@NOME_ITEM+'%'	
END